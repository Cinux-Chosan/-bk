import util from 'amazon/utils/util';
import { module, test } from 'qunit';

module('Unit | Utility | util', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = util();
    assert.ok(result);
  });
});
